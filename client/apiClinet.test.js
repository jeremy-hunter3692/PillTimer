const { getLastSession } = require('./apiClient')

describe('getLastSession', () => {
  test.todo('gets last session from the database') // () => {
  //   // expect.assertions(7)
  //   return getLastSession(testDb).then((tracks) => {
  //     expect(tracks).toHaveLength(1)
  //   })
  // })
})
